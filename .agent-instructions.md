# Instructions pour l'Agent IA

- **NE JAMAIS** faire de `git push` sans s'être assuré que le code compile (`pnpm build`).
- **NE JAMAIS** faire de `git push` sans que les tests passent (`node test.cjs`).

Un hook git `pre-push` a été ajouté pour forcer ces vérifications.
